---
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 1haoji:8848
      config:
        server-addr: 1haoji:8848
        file-extension: yaml
        ext-config:
          - {group: seata-verify , data-id: seata-verify.yaml}
          - {group: seata-verify , data-id: seata-verify-order.yaml}
        group: seata-verify
